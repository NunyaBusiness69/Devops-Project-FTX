---
- hosts: localhost
  become: true
  vars:
    ansible_python_interpreter: /usr/bin/python3

  tasks:
    - name: Start Minecraft + backup containers via docker-compose
      community.docker.docker_compose:
        project_src: /opt/minecraft
        state: present
